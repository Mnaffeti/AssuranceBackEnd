spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_exam
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update  # Or update, if you're managing schema updates
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect  # Corrected the dialect
